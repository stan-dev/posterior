#' @export
summary.draws_matrix <- function(object, ...) {
  summarise_draws(object, ...)
}

#' @export
summary.draws_array <- function(object, ...) {
  summarise_draws(object, ...)
}

#' @export
summary.draws_data_frame <- function(object, ...) {
  summarise_draws(object, ...)
}
