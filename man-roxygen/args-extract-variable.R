#' @param variable (string) The name of the variable to extract. Must include
#' indices for array variables (e.g. `"x[1]"`, `"y[1,2]"`). To extract all
#' dimensions from variables with indices, use [extract_variable_array()].
