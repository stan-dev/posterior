#' @param summary What style of summary to display: `"mean_sd"` displays `mean±sd`,
#' `"median_mad"` displays `median±mad`. If `NULL`, `getOption("posterior.rvar_summary")` is
#' used (default `"mean_sd`).
