#' @param dimnames Override for the names of the dimensions of the [`rvar`] to be created (see [dimnames()]): a list of character
#' vectors giving the names in each dimension. If `NULL` (the default), this is determined by the input.
#' **NOTE:** This argument controls the names of the dimensions of the [`rvar`], not the underlying array.
